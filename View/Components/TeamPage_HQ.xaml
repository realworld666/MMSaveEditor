<UserControl x:Class="MMSaveEditor.View.Components.TeamPage_HQ"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MMSaveEditor.View.TabPages"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <DataGrid CanUserAddRows="False" CanUserReorderColumns="False" RowDetailsVisibilityMode="Visible" SelectionMode="Single" SelectionUnit="Cell" AutoGenerateColumns="True" ItemsSource="{Binding Mode=OneWay}" CellEditEnding="DataGrid_CellEditEnding">
            
        </DataGrid>
        <!--<ListView ItemTemplate="{DynamicResource InventoryItem}" ItemsSource="{Binding EngineParts, Mode=OneWay}">
                            </ListView>-->
    </Grid>
</UserControl>
